{
  plugins.claudecode = {
    enable = true;
    settings = {
      diff_opts = {
        layout = "vertical";
        open_in_new_tab = true;
      };
      terminal.provider = "snacks";
    };
  };
}
