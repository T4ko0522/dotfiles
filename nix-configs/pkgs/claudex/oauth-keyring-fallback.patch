diff --git a/src/oauth/providers.rs b/src/oauth/providers.rs
index 9611b9f..a7cad6a 100644
--- a/src/oauth/providers.rs
+++ b/src/oauth/providers.rs
@@ -258,8 +258,12 @@ async fn login_chatgpt_browser(profile_name: &str) -> Result<()> {
             .await?;
 
     // 存储到 keyring 并回写 ~/.codex/auth.json
-    super::source::store_keyring(profile_name, &token)?;
     super::source::write_codex_credentials_atomic(&token)?;
+    if let Err(err) = super::source::store_keyring(profile_name, &token) {
+        tracing::warn!(
+            "failed to store ChatGPT OAuth token in keyring for profile '{profile_name}': {err}"
+        );
+    }
 
     println!("ChatGPT OAuth token stored for profile '{profile_name}'.");
     Ok(())
@@ -287,8 +291,12 @@ async fn login_chatgpt_headless(profile_name: &str) -> Result<()> {
     )
     .await?;
 
-    super::source::store_keyring(profile_name, &token)?;
     super::source::write_codex_credentials_atomic(&token)?;
+    if let Err(err) = super::source::store_keyring(profile_name, &token) {
+        tracing::warn!(
+            "failed to store ChatGPT OAuth token in keyring for profile '{profile_name}': {err}"
+        );
+    }
 
     println!("ChatGPT OAuth token stored for profile '{profile_name}'.");
     Ok(())
