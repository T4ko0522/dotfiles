# T4ko0522/cf-edgeNix の binary cache へ NixOS closure を publish する。

name: publish-cache

on:
  workflow_call:
    inputs:
      host:
        description: "対象 host を 1 つだけ指定したい場合 (空なら matrix の全 host)"
        required: false
        type: string
  workflow_dispatch:
    inputs:
      host:
        description: "対象 host を 1 つだけ指定したい場合 (空なら matrix の全 host)"
        required: false
        type: string

concurrency:
  group: publish-cache-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    environment: production

    steps:
      - name: Checkout dotfiles (flake source)
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Checkout cf-edgeNix (publish tooling)
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          repository: T4ko0522/cf-edgeNix
          ref: 2212b89b1b455eb2d803c08ae3c2b85cf3700267
          path: .cf-edgenix

      # nixos-ci は CI 検証用なので publish 対象外、default は laptop の alias なので除外。
      # workflow_dispatch.inputs.host が空なら両方、指定があれば allowlist 内のもののみ。
      - name: Resolve target hosts
        id: hosts
        env:
          REQUESTED: ${{ inputs.host }}
        run: |
          if [ -z "$REQUESTED" ]; then
            echo "list=laptop desktop" >> "$GITHUB_OUTPUT"
          else
            case "$REQUESTED" in
              laptop|desktop)
                echo "list=$REQUESTED" >> "$GITHUB_OUTPUT"
                ;;
              *)
                echo "unknown host: $REQUESTED" >&2
                exit 1
                ;;
            esac
          fi

      # flake.nix の nixConfig.extra-substituters は trusted-user 制約で runner では効かないため、
      # ここで明示的に substituter / trusted-public-keys を inject する。
      # 通常の NixOS 構成 (nix-configs/feature/modules/core/nix.nix) と同じ cache を使う。
      # 自前 cache を優先し、前回 publish 済みの store path を再利用する。
      - name: Install Nix
        uses: cachix/install-nix-action@08dcb3a5e62fa31e2da3d490afc4176ef55ecd72 # v30
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            substituters = https://nix.t4ko.pet https://vicinae.cachix.org https://cache.numtide.com https://codex-desktop-linux.cachix.org https://cache.nixos.org/
            trusted-public-keys = nix.t4ko.pet-1:0eRO18L1/5diWYWboKKPTejQGhGCHNITwELiUaX7Kps= vicinae.cachix.org-1:1kDrfienkGHPYbkpNj1mWTr7Fm1+zcenzgTizIcI3oc= niks3.numtide.com-1:DTx8wZduET09hRmMtKdQDxNNthLQETkc/yaX7M4qK0g= codex-desktop-linux.cachix.org-1:nX/xy6AdK9hQE24A8ALGjkCKj2ObFmcnemiL5Cid4nk= cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY=
            accept-flake-config = true

      # 既存の /nix/store cache は復元にだけ使う。build結果は自前binary cacheへpublishされるため、
      # job後にstoreをGC・再圧縮してActions cacheへ二重保存しない。
      - name: Restore /nix/store cache
        uses: nix-community/cache-nix-action@7df957e333c1e5da7721f60227dbba6d06080569 # v7
        with:
          primary-key: nix-${{ runner.os }}-${{ hashFiles('flake.lock') }}
          restore-prefixes-first-match: nix-${{ runner.os }}-
          save: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76 # v2.0.2
        with:
          bun-version: "1.3.14"

      - name: Cache Bun install
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('.cf-edgenix/bun.lock', '.cf-edgenix/bun.lockb', '.cf-edgenix/package.json') }}
          restore-keys: |
            bun-${{ runner.os }}-

      - name: Install cf-edgeNix deps
        working-directory: .cf-edgenix
        run: bun install --frozen-lockfile

      # 全 host を 1 回の build / copy / upstream prune で publishする。
      # laptop と desktop の共通 closure を同じ CACHE_DIR で再利用し、二重圧縮を避ける。
      - name: Build, sign & publish
        env:
          HOSTS: ${{ steps.hosts.outputs.list }}
          CACHE_DIR: ${{ runner.temp }}/cf-edgenix-cache
          CACHE_PRIVATE_KEY: ${{ secrets.CACHE_PRIVATE_KEY }}
          API_BASE_URL: ${{ vars.API_BASE_URL }}
          ADMIN_TOKEN: ${{ secrets.ADMIN_TOKEN }}
          R2_BUCKET_NAME: ${{ vars.R2_BUCKET_NAME }}
          KV_NAMESPACE_ID: ${{ vars.KV_NAMESPACE_ID }}
          GIT_REV: ${{ github.sha }}
          SYSTEM: x86_64-linux
          FLAKE_LOCK_HASH: ${{ hashFiles('flake.lock') }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          trap 'rm -rf -- "$CACHE_DIR"' EXIT
          mkdir -p "$CACHE_DIR"
          read -r -a hosts <<< "$HOSTS"
          bash .cf-edgenix/scripts/publish.sh "${hosts[@]}"
